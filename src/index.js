import {BrowserRouter,Router,Switch,HashRouter,Match,Route,Link,hashHistory,IndexLink} from 'react-router-dom'
import React from 'react'
import ReactDOM from 'react-dom'
import BasicExample from './main'

ReactDOM.render(
    <BasicExample/>,
    document.getElementById('root')
);